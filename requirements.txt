# GLib main loop (import gi): install with apt, then use a venv with --system-site-packages
#   sudo apt install python3-gi gir1.2-glib-2.0
#   python3 -m venv .venv --system-site-packages
dbus-python==1.3.2
